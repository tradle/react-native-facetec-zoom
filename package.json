{
  "name": "react-native-facetec-zoom",
  "version": "1.2.0",
  "description": "unofficial react-native bridge for Facetec's Zoom SDK",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "eslint --quiet --ignore-path .gitignore .",
    "precommit": "npm run lint"
  },
  "keywords": [
    "react-native",
    "react-native-component",
    "liveness-detection",
    "zoom",
    "facetec",
    "tradle"
  ],
  "author": "mvayngrib",
  "license": "MIT",
  "devDependencies": {
    "eslint": "^5.5.0",
    "husky": "^0.14.3"
  },
  "peerDependencies": {
    "react-native-image-store": "*"
  }
}
